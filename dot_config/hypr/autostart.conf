exec-once=playerctld daemon # Audio handler
exec-once=ags # Launch GTK shell
exec-once=$XDG_CONFIG_HOME/hypr/scripts/wallpaper-handler.sh init # Launch wallpaper handler
exec-once=/usr/lib/polkit-kde-authentication-agent-1 # Start authentication agent
exec-once=hypridle # Idle manager
exec-once=wl-paste --type text --watch cliphist store # Clipboard manager
exec-once=clight --lat=$LATITUDE --lon=$LONGITUDE # Launch brightness controller
